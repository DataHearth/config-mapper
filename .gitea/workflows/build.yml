name: build

run-name: Build and test

on:
  push:
    branches:
      - "*"

jobs:
  build:
    name: Build and test

    runs-on: debian-go

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        run: go build -v ./...
